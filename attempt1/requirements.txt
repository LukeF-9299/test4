# Python packages
python-docx==0.8.11
pymupdf==1.24.5
openai==2.30.0

# System dependencies (install separately):
# - antiword (for .doc file support)
#   Ubuntu/Debian: sudo apt-get install antiword
#   macOS: brew install antiword
#   CentOS/RHEL: yum install antiword

# Optional system dependencies for enhanced PDF support:
# - poppler-utils (for additional PDF utilities)
#   Ubuntu/Debian: sudo apt-get install poppler-utils
#   macOS: brew install poppler
